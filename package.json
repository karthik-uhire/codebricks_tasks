{
  "name": "recipe-finder-root",
  "version": "1.0.0",
  "description": "Recipe Finder Application",
  "scripts": {
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev",
    "install-all": "npm --prefix backend install"
  }
}
